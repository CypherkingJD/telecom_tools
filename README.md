Metrics by level
L1 — what can actually be measured
DimensionMetricSourceAlert acknowledgment speedMean time alert-to-ack (by shift, by analyst)NMS / monitoring platform (Zabbix, SolarWinds)Alert classification accuracy% tickets recategorized by L2 after L1 creationTicketing system (ServiceNow, Jira, Remedy)Ticket quality% tickets returned to L1 for missing fieldsTicketing systemEscalation timingMean time to escalate P1/P2 vs. SLA thresholdTicketing system (ticket state timestamps)Escalation accuracy% L1 escalations accepted vs. sent back by L2Ticketing system (escalation return reason field)Shift handoff qualityOpen P1/P2 incidents dropped at shift change (no update for >30 min post-handoff)Ticketing systemSOP adherence% incidents where required checklist was attachedTicketing system / workflow enforcement
L1 data is almost entirely extractable from your ticketing system and NMS. The gap is usually that the ticketing system isn't enforcing structured fields — so the metric exists in theory but not in practice yet.

L2 — what can actually be measured
DimensionMetricSourceFault isolation speedMean time L2-owned to first hypothesis documented (ticket timestamp)Ticketing systemDiagnostic completeness% tickets with show output or log evidence attached before escalationTicketing systemChange execution qualityChange-induced incident rate (% of L2-executed changes causing an unplanned outage within 24h)Change management system + incident system (join on CI)Rollback success rate% change rollbacks completed within planned windowChange management systemTAC case qualityMean time from TAC case open to first vendor response (proxy for case quality — well-documented cases get faster responses)Vendor portal exports (Cisco CCO, Juniper CSP, etc.)L3 escalation accuracy% L2 escalations accepted vs. returned by L3 with "insufficient triage" tagTicketing systemRepeat incident rate% L2 incidents that are recurrences of a prior root cause within 90 daysTicketing system (problem record linkage)Post-change verification% changes with documented verification step completedChange management system
L2 data requires joining your ticketing system with your change management system — that join on configuration item (CI) is the key to change-induced incident rate, which is your most important L2 metric.

L3 — what can actually be measured
DimensionMetricSourceRCA completion rate% SEV1/SEV2 incidents with completed postmortem within SLA (72h)Ticketing system / postmortem trackerRCA depth scoreRubric-scored postmortem quality (root cause identified, contributing factors listed, timeline present, SMART actions)Postmortem review process — scored by a peer or managerCorrective action closure rate% postmortem action items closed within committed dateProject tracker (Jira, ServiceNow problem management)Recurrence rate% incidents where prior RCA action item was marked closed but same root cause recurredTicketing system (problem linkage + recurrence flag)Change design quality% L3-designed changes that pass peer review without revisionChange management system (review iteration count)Vendor defect resolutionMean time from defect filed to confirmed fix version available (L3 influence metric)Vendor portal + internal defect trackerL2 development impactL2 escalation return rate trend over time (is it improving as L3 coaches?)Ticketing systemKnowledge base contributionKB articles authored/updated per quarter per engineerKB platform (Confluence, SharePoint, ServiceNow KB)
L3 metrics are harder to automate because the most important ones (RCA depth, design quality) require human judgment. The practical approach is a lightweight rubric scored by a peer or the manager — which maps directly back to the assessment tool you already have.
